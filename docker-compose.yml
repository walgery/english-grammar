services:
  english-grammar:
    build: .
    image: english-grammar:latest
    container_name: english-grammar
    restart: unless-stopped
    ports:
      - "127.0.0.1:8501:8501"
    volumes:
      - ./data:/app/data
      - ./llm_config.json:/app/llm_config.json
